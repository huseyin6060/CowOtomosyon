<UserControl x:Class="CowMaster.Sayfalar.SonDurumlar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" d:DesignHeight="850" d:DesignWidth="1150">

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ÜST BAŞLIK VE BUTONLAR -->
        <Grid Grid.Row="0" Margin="0,0,0,25">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="55" Height="55" Background="#2F80ED" CornerRadius="15">
                    <materialDesign:PackIcon Kind="ViewDashboard" Foreground="White" Width="32" Height="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Margin="15,0,0,0">
                    <TextBlock Text="Genel Son Durum" FontSize="26" FontWeight="ExtraBold" Foreground="#1E293B"/>
                    <TextBlock Text="Hüseyin Yüce, işletmen anlık olarak takip ediliyor." FontSize="14" Foreground="#64748B"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Style="{StaticResource MaterialDesignOutlinedButton}" Click="Raporindir_Click" Content="Rapor İndir" Margin="0,0,10,0" BorderBrush="#CBD5E1"/>
            </StackPanel>
        </Grid>

        <!-- İSTATİSTİK KARTLARI -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="-8,0">
            <!-- Toplam Hayvan -->
            <materialDesign:Card Margin="8" Padding="20" UniformCornerRadius="20" Background="White">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Cow" Foreground="#2F80ED" Width="24" Height="24"/>
                    <TextBlock Text="Toplam Hayvan" Foreground="#64748B" FontSize="14" Margin="0,10,0,0"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding ToplamHayvan, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text=" Baş" FontSize="16" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="5,0,0,5"/>
                    </StackPanel>
                    <ProgressBar Value="{Binding KapasiteOrani, Mode=OneWay}" Foreground="#2F80ED" Height="6" Margin="0,15,0,0" materialDesign:TransitionAssist.DisableTransitions="True"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Günlük Süt -->
            <materialDesign:Card Margin="8" Padding="20" UniformCornerRadius="20" Background="White">
                <StackPanel>
                    <materialDesign:PackIcon Kind="Water" Foreground="#27AE60" Width="24" Height="24"/>
                    <TextBlock Text="Günlük Süt Verimi" Foreground="#64748B" FontSize="14" Margin="0,10,0,0"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding GunlukSut, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text=" Litre" FontSize="16" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="5,0,0,5"/>
                    </StackPanel>
                    <TextBlock Text="{Binding SutTrend, Mode=OneWay}" FontSize="12" FontWeight="SemiBold" Foreground="#27AE60" Margin="0,10,0,0"/>
                    <ProgressBar Value="{Binding KapasiteOrani, Mode=OneWay}" Foreground="Green" Height="6" Margin="0,5,0,0" materialDesign:TransitionAssist.DisableTransitions="True"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Aktif Tedaviler -->
            <materialDesign:Card Margin="8" Padding="20" UniformCornerRadius="20" Background="#FFF1F2">
                <StackPanel>
                    <materialDesign:PackIcon Kind="AlertCircle" Foreground="#E11D48" Width="24" Height="24"/>
                    <TextBlock Text="Aktif Tedaviler" Foreground="#9F1239" FontSize="14" Margin="0,10,0,0"/>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding AktifTedaviSayisi, Mode=OneWay}" FontSize="32" FontWeight="Bold" Foreground="#E11D48"/>
                        <TextBlock Text=" Hayvan" FontSize="16" Foreground="#FB7185" VerticalAlignment="Bottom" Margin="5,0,0,5"/>
                    </StackPanel>
                    <TextBlock Text="Hemen kontrol etmeniz önerilir" FontSize="12" Foreground="#E11D48" Margin="0,10,0,0"/>
                    <ProgressBar Value="{Binding KapasiteOrani, Mode=OneWay}" Foreground="Red" Height="6" Margin="0,5,0,0" materialDesign:TransitionAssist.DisableTransitions="True"/>
                </StackPanel>
            </materialDesign:Card>
        </UniformGrid>

        <!-- SON HAREKETLER TABLOSU -->
        <materialDesign:Card Grid.Row="2" materialDesign:ElevationAssist.Elevation="Dp3" UniformCornerRadius="25" Margin="0,20,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="SlateGray" CornerRadius="25,25,0,0" Padding="15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="History" Foreground="#1A237E" VerticalAlignment="Center" Width="26" Height="26" Margin="5,0,10,0"/>
                            <TextBlock Text="Son Hareketler" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" Foreground="Black" Margin="10,0,20,0"/>
                            <TextBox x:Name="txtLogAra" Width="250" TextChanged="txtLogAra_TextChanged"
                             materialDesign:HintAssist.Hint="Olay ara..." 
                             Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                             materialDesign:TextFieldAssist.TextFieldCornerRadius="15" 
                             Background="White" materialDesign:TextFieldAssist.HasClearButton="True"
                             Height="40" Padding="10,5"/>
                        </StackPanel>
                        <Button Click="btnLogYenile_Click" Grid.Column="1" Style="{StaticResource MaterialDesignFlatMidBgButton}" Width="50" Height="40" ToolTip="Geçmişi Güncelle">
                            <materialDesign:PackIcon Kind="Autorenew" Width="22" Height="22"/>
                        </Button>
                    </Grid>
                </Border>

                <DataGrid x:Name="dgSonHareketler" Grid.Row="1" Margin="10,5,10,10" 
          AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" 
          Background="Transparent" BorderThickness="0" RowHeight="50"
          Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="TARİH" Binding="{Binding zaman, StringFormat={}{0:dd.MM.yyyy HH:mm}}" Width="140"/>
                        <DataGridTextColumn Header="İŞLEM TÜRÜ" Binding="{Binding olay_baslik}" Width="180" FontWeight="Bold"/>
                        <DataGridTextColumn Header="DETAYLI BİLGİ" Binding="{Binding detay}" Width="*"/>
                        <DataGridTextColumn Header="SORUMLU" Binding="{Binding personel}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>